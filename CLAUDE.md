# CLAUDE.md

このプロジェクトの行動規約は `AGENTS.md` を正本とする。**まずそれを読むこと。**

@AGENTS.md

---

## Claude Code 固有の補足

### 役割分担

このプロジェクトでは **Claude = 設計 / CodeX = 実装** を基本としている。

- Claude が呼ばれるのは主に「設計」「レビュー」「方針判断」の場面
- 実装を依頼された場合も、`AGENTS.md` の規約は同じように適用される
- 設計に変更が必要だと判断したら、コードではなく
  `ai_tasks/20260804_sourceglass_mvp_design/` のドキュメントを先に更新する

### 探索の順序

1. `ai_tasks/context_snapshot.md` — 現在地
2. 該当する設計ドキュメント
3. コード

**設計ドキュメントを読まずにコードから推測しない。** 決定の理由はコードに書かれていない。

### レビュー時に特に見るところ

`AGENTS.md` §1 の違反は、動作していても差し戻す。

- 確率・スコア表示が紛れ込んでいないか
- `✓` や緑が結果表示に使われていないか
- 免責文言が折りたためるようになっていないか
- `copy.md` の文言が言い換えられていないか
- `present` / `absent` / `error` の3状態が潰されていないか
- 外部オリジンへの参照が増えていないか
- `features/provenance/` に React / DOM 依存が入っていないか

### ドキュメント更新

`ai_tasks/` 配下を更新したら `context_snapshot.md` も同じ作業内で更新する。
`# Last Updated:` の行を忘れない。
